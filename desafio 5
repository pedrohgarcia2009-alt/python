# Desafio 05: Validador de Senha Simples 

def validar_senha(senha: str) -> list[str]:
    """
    Valida a senha de acordo com critérios básicos.
    Retorna uma lista de mensagens de erro, ou vazia se a senha for válida.
    """
    erros = []
    if len(senha) < 8:
        erros.append("➡ A senha deve ter pelo menos 8 caracteres.")
    if not any(c.isdigit() for c in senha):
        erros.append("➡ A senha deve conter pelo menos 1 número.")
    if not any(c.isupper() for c in senha):
        erros.append("➡ A senha deve conter pelo menos 1 letra maiúscula.")
    if not any(c.islower() for c in senha):
        erros.append("➡ A senha deve conter pelo menos 1 letra minúscula.")
    return erros

print("=== Validador de Senha ===")
senha = input("Digite sua senha: ")

erros = validar_senha(senha)

if erros:
    print("\n❌ Senha inválida! Corrija os seguintes pontos:")
    for erro in erros:
        print(erro)
else:
    print("✅ Senha válida e segura!")



